<%- include('../partials/header') %>

<div class="page-actions">
  <div>
    <h1><%= child.name %></h1>
    <div class="sub"><%= child.class_name || '' %> · السجل الصحي (للاطلاع فقط)</div>
  </div>
  <a href="/teacher/attendance" class="btn btn-secondary"><i class="fa-solid fa-arrow-right"></i> عودة</a>
</div>

<div class="card mt-0">
  <p class="text-muted" style="line-height:1.8;">
    هذه البيانات مسجّلة من إدارة الروضة أو ولي الأمر لضمان سلامة الطفل — للاطلاع فقط، ولا يمكن تعديلها من هنا.
  </p>
</div>

<%- include('../partials/health-record', { healthProfile, contacts, editable: false, actionPrefix: '' }) %>

<%- include('../partials/footer') %>
